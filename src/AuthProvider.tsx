// src/AuthProvider.tsx
import React from "react";

const AuthProvider = ({ children }: { children: React.ReactNode }) => {
  // Implement your context provider logic if needed
  return <>{children}</>;
};

export default AuthProvider;
